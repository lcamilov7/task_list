<div class="flex items-center justify-center min-h-screen">
  <div class="bg-white p-8 rounded-xl shadow-lg w-full max-w-md">
    <!-- Tu contenido aquí -->
    <h1 class="text-2xl font-bold mb-4">Lista de tareas</h1>
    <!-- etc. -->
    <%= link_to 'Nueva', new_task_path, class: 'btn btn-primary', data: { turbo_stream: true } %>
    <div class="">
      <% @tasks.each do |task|%>
        <%= render(Tasks::CardComponent.new(task:)) %>
      <% end %>
    </div>
  </div>
  <div id="task_drawer_container"></div>
</div>
